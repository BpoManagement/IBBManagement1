export interface Service {
  title: string;
  description: string;
  icon: string;
}

export interface Tool {
  title: string;
  description: string;
}

export interface Expertise {
  title: string;
  items: string[];
}
